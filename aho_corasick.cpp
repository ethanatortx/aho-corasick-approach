#include "aho_corasick.h"

namespace ac = aho_corasick;

void ac::build(const std::vector<std::string>& dict)
{
	
	for(std::vector<std::string>::const_iterator i = dict.begin(); i != dict.end(); ++i)
	{

	}
}